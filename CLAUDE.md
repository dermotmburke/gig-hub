# Claude Code guidance for gig-hub

## Before committing

- Always run `mvn test` — coverage is enforced at 90% and will fail the build if dropped
- Always update `README.md` when making architectural changes (new notifiers, config properties, structural changes, deployment model)
