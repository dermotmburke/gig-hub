<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level %logger{36} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Keep expected error/info logs from negative-path tests out of normal test output. -->
    <logger name="com.d3bot.events.pipelines.EventPipeline" level="OFF"/>
    <logger name="com.d3bot.events.notifiers.HeartbeatEventNotifier" level="OFF"/>
    <logger name="com.d3bot.events.notifiers.SlackEventNotifier" level="OFF"/>
    <logger name="com.d3bot.events.notifiers.LoggingEventNotifier" level="OFF"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
